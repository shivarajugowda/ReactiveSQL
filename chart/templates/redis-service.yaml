apiVersion: v1
kind: Service
metadata:
  name: {{ template "ReactiveSQL.fullname" . }}-redis
  labels:
    component: redis
{{ include "ReactiveSQL.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.redis.type }}
  ports:
    - port: {{ .Values.service.redis.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ReactiveSQL.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: redis