apiVersion: v1
kind: Service
metadata:
  name: {{ template "kuper.fullname" . }}-web
  labels:
    component: web
{{ include "kuper.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.web.type }}
  ports:
    - port: {{ .Values.service.web.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "kuper.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: web
